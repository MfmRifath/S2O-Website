class TermModal{
    termId: number;
    termName: string;

    constructor (termId: number,
        termName: string){
            this.termId=termId;
            this.termName=termName;
    }
}

export default TermModal;